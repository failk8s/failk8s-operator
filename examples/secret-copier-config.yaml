apiVersion: failk8s.dev/v1alpha1
kind: SecretCopierConfig
metadata:
  name: registry-credentials
spec:
  secrets:
  - name: training-getwarped-org-tls
    namespace: eduk8s
    targetNamespaces:
      nameSelector:
        matchNames:
        - example-1
        - example-2
    newName: faik8s-tls-secret
    applyLabels:
      failk8s-tls-secret: "yes"
